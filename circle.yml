dependencies:
  pre:
    - gem uninstall bundler
    - gem install bundler
  post:
    - bundle exec appraisal install
test:
  override:
    - bundle exec appraisal rake test
database:
  override:
    - bundle exec rake -T
